---
name: First workflow for the DevOps 

on:
  workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cat firstfile.txt
      - run: date
      - run : echo "The current branch is ${{ github.ref_name }}"
...
